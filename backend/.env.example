PORT = <PORT NUMBER HERE>
CORS_ORIGIN = <CORS ORIGIN HERE>

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=final_yr_proj
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/final_yr_proj

# Cache (Valkey)
VALKEY_HOST=localhost
VALKEY_PORT=6379
VALKEY_URL=redis://localhost:6379